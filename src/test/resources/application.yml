spring:
  data:
    mongodb:
      database: test
  banner:
    location: banner/taskListBanner.txt
  output:
    ansi:
      enabled: ALWAYS
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
