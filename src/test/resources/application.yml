spring:
  data:
    mongodb:
      database: test
  banner:
    location: banner/practicelibBanner.txt
  output:
    ansi:
      enabled: ALWAYS
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
